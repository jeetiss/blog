language: node_js
node_js:
  - "10"

branches:
  only:
  - dev

install:
  - npm install

script:
  - npm run build

deploy:
  provider: pages
  skip-cleanup: true
  keep-history: true
  local-dir: ./public
  target-branch: master
  github-token: $GITHUB_TOKEN
  on:
    branch: dev
